# 📝 NHẬT KÝ NÂNG CẤP & BẢO TRÌ HỆ THỐNG (22/09/2026)
## DỰ ÁN: UNIVERSAL LEAD AGENT & B2B CRM ENGINE

---

## 📌 TỔNG QUAN CÁC CÔNG VIỆC ĐÃ HOÀN THÀNH HÔM NAY

Trong ngày **22/09/2026**, hệ thống **Universal Lead Agent Engine** đã được phân tích, sửa lỗi toàn diện và tối ưu hóa ở 5 hạng mục quan trọng:

1. **Khắc phục triệt để lỗi thiếu dữ liệu / ô trắng** khi bơm Lead vào Google Sheet của từng khách hàng.
2. **Sửa lỗi Firecrawl Crawler luôn bị 0 bài (Fallback lỗi)** do thiếu cấu hình `scrapeOptions`.
3. **Chuẩn hóa 100% dữ liệu các trường**, lọc triệt để URL dán nhầm vào cột SĐT và loại bỏ hoàn toàn chữ rác (`N/A`, `null`, `undefined`).
4. **Tương thích đa phiên bản Webhook Google Apps Script** bằng cách truyền song song thuộc tính gốc và thuộc tính bí danh (alias keys).
5. **Chẩn đoán & Cảnh báo phân quyền Google Sheet** khi Google Apps Script Webhook trả về HTML trang lỗi từ Google.

---

## 🛠 CHI TIẾT 5 SỰ CỐ & GIẢI PHÁP KỸ THUẬT

### 1. Lỗi đứt đoạn dữ liệu AI (Single-Batch Output Truncation)
* **Hiện tượng:** Có khách hàng nhận đủ Lead, có khách hàng bị nhận 0 Lead hoặc mất dữ liệu.
* **Nguyên nhân:** Khi một khách hàng có lượng bài cào thô lớn (30–60 bài), gửi toàn bộ vào 1 request Gemini AI vượt quá giới hạn token đầu ra, khiến phản hồi JSON bị ngắt giữa chừng (`SyntaxError`).
* **Giải pháp:**
  * Thêm cơ chế **Chunking (`CHUNK_SIZE = 15`)**: Chia danh sách bài viết thô thành từng lượt tối đa 15 bài/lượt gửi AI.
  * Bổ sung hàm Regex trích xuất mảng JSON an toàn phòng trường hợp AI phản hồi kèm ký tự thừa.

### 2. Lỗi dán nhầm URL vào cột SĐT & Rác dữ liệu
* **Hiện tượng:** Cột SĐT (Cột I) dính link `threads.net`, `tiktok.com`, `voz.vn`, `youtube.com` hoặc chữ `N/A`, `null`.
* **Nguyên nhân:** Hàm `cleanPhoneNumber` chỉ mới kiểm tra `facebook.com` và `http://`.
* **Giải pháp:**
  * Nâng cấp `cleanPhoneNumber` quét sạch 100% domain, URL xã hội và chuỗi rác (`null`, `n/a`, `undefined`, `không`, `chưa có`), tự động thay bằng `'Chưa có SĐT (Inbox qua link bài)'`.
  * Thêm helper `cleanStringField` lấp đầy giá trị mặc định cho cả 10 cột dữ liệu.

### 3. Lỗi Firecrawl Crawler trả về 0 kết quả
* **Hiện tượng:** Mọi lệnh cào bằng Firecrawl dự phòng đều trả về 0 bài mặc dù Secret API Key đã có trên GitHub.
* **Nguyên nhân:** Payload gửi tới `https://api.firecrawl.dev/v1/search` thiếu `scrapeOptions: { formats: ['markdown'] }` và gửi sai `searchOptions: { limit: 10 }`. Firecrawl v1 API không cào nội dung markdown (`item.markdown` bị `undefined`), làm hỏng điều kiện `if (item.url && item.markdown)`.
* **Giải pháp:** Cập nhật payload chuẩn v1 API:
  ```json
  {
    "query": query,
    "limit": 10,
    "scrapeOptions": { "formats": ["markdown"] }
  }
  ```
  Thêm fallback lấy nội dung `item.markdown || item.description || item.snippet`.

### 4. Lỗi Mismatch Tên Thuộc Tính Webhook (Field Compatibility)
* **Hiện tượng:** Một số cột như Tiêu đề (F), Chi tiết (G), Ngân sách (H) bị trắng dù AI đã trích xuất.
* **Nguyên nhân:** Webhook Google Apps Script của từng file mẫu có thể đọc các tên key khác nhau (`score`, `content`, `extra1`, `phone`, `link`...).
* **Giải pháp:** Mở rộng interface `ExtractedItem`, gửi đồng thời cả key gốc và key alias:
  * `scanTime` & `date`
  * `postedAgo` & `time`
  * `categoryTag` & `tag` & `category`
  * `scoreOrPriority` & `score` & `priority`
  * `title` & `summary`
  * `contentOrBrief` & `content` & `brief` & `description`
  * `extraField1` & `extra1` & `budget`
  * `extraField2` & `extra2` & `phone` & `contact`
  * `url` & `link`

### 5. Phát hiện & Xử lý File Sheet bị lỗi phân quyền Google Workspace
* **Hiện tượng:** File Sheet riêng của khách `heoquay` (`1NYD5h1U3TCvZc6cghl7zh0uplkDZ13i0pQRrU-0nByY`) không nhận được dữ liệu.
* **Nguyên nhân:** Log kiểm tra cho thấy Google Webhook trả về HTML trang lỗi của Google khi gọi `SpreadsheetApp.openById` do File Sheet bị thiếu quyền Editor cho Webhook Apps Script.
* **Giải pháp:**
  * Bổ sung log cảnh báo tự động phát hiện phản hồi HTML lỗi.
  * Hướng dẫn khắc phục 2 bước trên Admin Dashboard (Tạm dừng -> Bật lại để tự sinh file chuẩn hoặc phân quyền Editor thủ công).

---

## 🗂 CẤU TRÚC GIT COMMITS ĐÃ PUSH HÔM NAY

Toàn bộ mã nguồn đã được commit và push thành công lên repository chính (`origin/main`):

1. **Commit `cffdd0e`**: `fix: resolve missing client lead data via post chunking and data normalization`
   *(Thêm chunking 15 bài/lượt, lọc sạch SĐT, chuẩn hóa dữ liệu khách hàng từ Admin).*
2. **Commit `628ea3e`**: `fix: update Firecrawl API payload with scrapeOptions to ensure markdown extraction`
   *(Sửa payload Firecrawl v1 API với limit 10 và scrapeOptions markdown).*
3. **Commit `2f9cdc4`**: `fix: add alias key mappings for full webhook compatibility and detect html error responses`
   *(Bổ sung alias key tương thích 100% với Webhook và cảnh báo HTML error).*

---

## 📋 HƯỚNG DẪN TRA CỨU & VẬN HÀNH KHI QUAY LẠI LÀM VIỆC

Khi quay lại làm việc trên dự án này, bạn có thể thực hiện các thao tác sau:

### 1. Kích hoạt và theo dõi Workflow trên GitHub
```powershell
# Chạy kích hoạt thủ công Workflow
gh workflow run runner.yml

# Xem danh sách các lượt chạy gần nhất
gh run list --limit 5

# Xem chi tiết log chạy thực tế (thay <run-id> bằng ID lượt chạy)
gh run view <run-id> --log
```

### 2. Quy trình thêm khách hàng mới (Không cần đụng code)
1. Mở file Google Sheet `Lead_Admin_Dashboard`.
2. Vào tab `CRM`, nhập thông tin khách hàng và chốt đơn.
3. Chuyển sang tab `Clients`, kiểm tra Mã ngành (Niche Key) và bật Trạng thái sang **`Đang hoạt động`**.
4. Hệ thống sẽ tự động chạy định kỳ mỗi 15 phút, cào lead và bơm thẳng vào Google Sheet riêng của khách.

---
*Nhật ký này được lưu tự động để phục vụ tra cứu lâu dài cho dự án.*
